2026-07-15  Alfonso Tatarelli <alfonso.tatarelli@link.it>

  * [Frontend] feat(shared-ui): nuovo `lnk-select` — dropdown con la UI del
    menu profilo (pannello flottante che esce dai contenitori, chiusura su
    click-fuori/Escape/scroll, navigazione da tastiera, apertura verso l'alto
    se in fondo manca spazio; trigger `fit-content` con min/max-width).
    Ricerca/filtro, selezione multipla, opzioni ricche (descrizione, icona,
    disabilitate) e gruppi. Sostituisce tutti i `<select>` nativi delle form
    (tipi pendenza, entrate, domini, applicazioni, intermediari); nel select
    inoltro le applicazioni mostrano il `principal` come descrizione.
    **Promosso in `@linkit/shared-ui` 0.27.0** (via `sync:lib`); l'interfaccia
    opzione è `LnkSelectOption` (per non collidere con `lnk-select-input`).

  * [Frontend] feat(tipi-pendenza): dettaglio config a campi completi. Nuovo
    `@core/ui/config-field-view`: ogni campo mostra un badge Configurato /
    Non configurato (a destra); i configurati rivelano il contenuto solo su
    richiesta, in un riquadro ad altezza fissa scrollabile con pulsante
    "Copia". Nel tab Altre funzioni, richiesta/risposta sono raggruppate sotto
    "Parser tracciati CSV", distinte dalla personalizzazione dettaglio.

  * [Frontend] feat(tipi-pendenza): nella maschera il `lnk-code-field`
    compilato è evidenziato con un bordo sinistro coral; il tab attivo è
    condiviso tra dettaglio e form (modifica/annulla/salva mantengono la
    scheda visualizzata via query param `tab`).

  * [Frontend] fix(entrate): ripristinata la spaziatura dei campi nella form.

  * [Frontend] feat(liste): persistenza opzionale dello stato lista (filtri +
    ordinamento) oltre il refresh, attivabile per feature col flag
    `persistState` nel config della lista (es. `pendenze-config.json`, dove è
    abilitato). Il valore è offuscato in `sessionStorage`/`localStorage` (non
    in chiaro, es. filtri con PII). Via `@linkit/shared-ui 0.28.0`
    (`ListStateService` persistente, `DisplayConfig.persistState`).

2026-07-14  Alfonso Tatarelli <alfonso.tatarelli@link.it>

  * [Frontend] feat(tipi-pendenza): editing dei template ripensato in unità
    "tipo → template". Nuovo `@core/ui/template-unit`: ogni template vive in un
    riquadro col proprio tipo, badge del formato (JSON / Freemarker) derivato
    dal tipo, stato vuoto quando il tipo non è scelto, ed editor. Adottato in
    tutti i tab config (portale backoffice/pagamento: definizione,
    impaginazione, validazione, trasformazione; tracciato CSV: intestazione +
    richiesta/risposta; visualizzazione) e nei promemoria dell'avvisatura
    (oggetto/messaggio gated dal tipo).

  * [Frontend] feat(shared-ui): nuovo `@core/ui/code-field` (sostituisce
    `json-field`) — editor per contenuti JSON / Freemarker / testo, nascosto di
    default (mostra su richiesta), con caricamento file + **drag & drop** e
    reset. I valori sono memorizzati come **stringa base64**: decodifica in
    lettura e codifica in scrittura (`@core/utils/base64`).

  * [Frontend] feat(shared-ui): nuovo `@core/ui/select` (`lnk-select`) —
    dropdown con la UI del menu profilo (pannello flottante che esce dai
    contenitori, chiusura su click-fuori/Escape/scroll, navigazione da
    tastiera). Sostituisce i `<select>` nativi della maschera tipi pendenza.

  * [Frontend] feat(form): barra delle azioni (salva/annulla) **sticky in
    basso** nelle form di edit (`@core/ui/form-action-bar`): resta visibile
    mentre la form scorre; bordo e ombra solo quando è ancorata. Adottata in
    tutte le form a pagina intera (tipi pendenza, domini, operatori,
    applicazioni, ruoli, entrate, intermediari).

2026-07-12  Alfonso Tatarelli <alfonso.tatarelli@link.it>

  * [Frontend] feat(tipi-pendenza): gestione completa della configurazione
    (`TipoPendenzaConfig`) nella maschera di dettaglio, ispirata alla console
    legacy (i blocchi config sono stringhe base64 di contenuti JSON o template
    freemarker). Form ristrutturata a tab: Dati · Portale backoffice · Portale
    pagamento · Avvisatura email · Avvisatura App IO · Altre funzioni.
    - Portali (backoffice/pagamento): form (tipo layout + definizione/
      impaginazione), validazione (JSON Schema), trasformazione (freemarker +
      template), inoltro (applicazione da `/applicazioni`).
    - Avvisatura email/App IO: promemoria avviso/scadenza/ricevuta (tipo,
      oggetto, messaggio, abilitato; `allegaPdf` solo mail, `soloEseguiti` sulla
      ricevuta, `preavviso` sulla scadenza).
    - Altre funzioni: parser tracciato CSV (tipo, intestazione, richiesta,
      risposta) e personalizzazione dettaglio (`visualizzazione`).
    Il salvataggio preserva le chiavi non modellate e omette i blocchi vuoti.

  * [Frontend] feat(tipi-pendenza): vista di dettaglio (read-only) arricchita
    con la stessa struttura a tab della maschera. I blocchi config sono resi in
    forma leggibile — info-grid dei campi scalari (abilitato, tipo layout/
    template, inoltro; flag `allegaPdf`/`soloEseguiti`/`preavviso` sui
    promemoria) e blocchi JSON etichettati (definizione, impaginazione,
    validazione, template, oggetto/messaggio, tracciato) — al posto del dump
    JSON grezzo. I blocchi assenti mostrano "Non configurato".

  * [Frontend] feat(shared-ui): nuovo `@core/ui/json-field` — editor JSON
    (textarea con validazione di sintassi, "carica da file" con pretty-print,
    svuota) come `ControlValueAccessor` con valore = oggetto parsato. Usato dai
    campi struttura dei tipi pendenza.

2026-07-07  Alfonso Tatarelli <alfonso.tatarelli@link.it>

  * [Frontend] feat(applicazioni, operatori): impostazione password
    (aggiornamento spec 2026-07-07). Nuovi endpoint write-only
    `PUT /applicazioni/{idA2A}/password` e `PUT /operatori/{principal}/password`;
    componente condiviso `@core/ui/set-password-card` (validazione policy +
    conferma, gestione 403). Sezione "Password" nei rispettivi dettagli.

  * [Frontend] feat(liste): ordinamento dalla search-pill. Nuovo dropdown di
    ordinamento configurabile (`showSort` + `sortOptions`) con selezione del
    campo e scorciatoia di direzione (icona cliccabile), reso come pulsante
    segmentato coerente col pulsante filtri. Abilitato su tutte le liste con
    `sort` documentato: tipi pendenza, entrate, domini, operatori, applicazioni,
    ruoli, intermediari, pendenze, ricevute.

  * [Frontend] feat(liste): ordinamento anche dagli header di tabella su tutte
    le colonne con campo `sort` supportato dall'API (non solo dalla search-pill).
    Ordinamento di default per nome descrittivo anziché per codice/id
    (intermediari `denominazione`, domini `ragioneSociale`, tipi pendenza ed
    entrate `descrizione`). Allineati i campi sort dove la chiave colonna
    differiva dal campo API (operatori `nome`, intermediari `codIntermediario`);
    aggiunto il sort da header anche alla tabella ricevute.

  * [Frontend] feat(liste): densità della search-pill selezionabile dal pannello
    tweaks (compact/regular/comfortable), default `compact`.

  * [Frontend] feat(ricevute): navigazione annidata alla pendenza collegata
    (rotta `:idDominio/:iuv/:idRicevuta/pendenza/:idA2A/:idPendenza`) con
    breadcrumb multilivello (Ricevute › IUV › Pendenza) e ritorno alla ricevuta.

  * [Frontend] fix(domini): dettaglio più completo. Il tab Dati mostra le
    sezioni Anagrafica e Contatti; le sotto-risorse (unità operative, conti di
    accredito, entrate, tipi pendenza) mostrano tutti i campi sia in vista sia
    in modifica (dettaglio caricato una volta e condiviso), incluso
    `autStampaPosteItaliane` nei conti di accredito.

  * [Frontend] style(anagrafiche): riordino delle voci nav (Intermediari in
    cima, Entrate in fondo) e form a larghezza piena.

  * [Frontend] feat(shared-ui): `lnk-search-pill` estesa (densità, dropdown di
    ordinamento, hover chip garantito visibile anche col tema GovPay) e
    `lnk-page-header` con azioni allineate in alto. Modifiche portate in
    upstream e sincronizzate via `sync:lib` (@linkit/shared-ui 0.24.3).

  * [Frontend] feat(shared-ui): `lnk-info-grid` con dimensione (S/M/L)
    regolabile a runtime da un controllo globale del pannello tweaks
    ("Dimensione dettagli", default `sm`) — si applica a tutte le griglie
    informative dei dettagli. Via `sync:lib` (@linkit/shared-ui 0.25.0).

2026-07-06  Alfonso Tatarelli <alfonso.tatarelli@link.it>

  * [Frontend] feat: estensione della GovPay Console API V2 a tutte le
    anagrafiche e alle ricevute (openapi 2026-07-06). Update via PUT
    (replace con If-Match/ETag), migrazione in-place delle feature V1
    (rimozione dei client `*.api.ts` sostituiti). Infrastruttura comune
    (Fase 0): `ConsoleApiService` esteso con `putBlob`/`putVoid`/`delete`;
    nuovi modelli condivisi `@core/models` (`connettore`, `acl`, `ref`) e
    componenti riusabili `@core/ui` (`acl-editor`, `ref-multiselect`,
    `inline-edit-card`).

  * [Frontend] feat(entrate): nuova sezione Entrate (tipi entrata globali)
    su API V2 con CRUD (lista search-pill, dettaglio, form nuovo/modifica).

  * [Frontend] feat(ruoli, operatori, applicazioni): migrazione in-place a
    V2. Ruoli e Operatori con editor ACL condiviso (R/W per servizio) e
    multi-select riferimenti (domini/tipiPendenza/ruoli con toggle "Tutti").
    Applicazioni con connettore-integrazione singleton (GET/PUT + credenziali)
    e codifica avvisi.

  * [Frontend] feat(tipi-pendenza): migrazione a V2. Form sui campi core con
    PUT che preserva invariate le sotto-strutture ricche non modellate.

  * [Frontend] feat(domini): nuova sezione Domini (enti creditori) su V2 con
    CRUD, logo binario (GET/PUT/DELETE, ≤256KB) e dettaglio a tab per le
    sotto-risorse: Unità operative, Conti di accredito, Entrate del dominio,
    Tipi pendenza del dominio e Connettori di notifica (mypivot, secim,
    govpay, hypersic-apk, maggioli-jppa) con credenziali write-only. Gestione
    inline (view/edit + creazione) con ETag/If-Match e preservazione dei
    campi non modellati nei replace.

  * [Frontend] feat(ricevute): migrazione a V2 in sola consultazione
    (read-only). Lista con search-pill (iuv/idRicevuta/idDominio + range
    date) e dettaglio per tupla `(idDominio, iuv, idRicevuta)` con download
    RT (PDF/XML/JSON) e RPT (XML/JSON) via content-negotiation, pendenza
    collegata e segnalazioni. Rimosso il client V1 `ricevute.api.ts`.

  * [Frontend] chore: pulizia (Fase 8). Rimozione del codice V1 sostituito,
    rimozione di dead code nelle rotte, allineamento voci nav e flag
    `MENU_*`, pulizia chiavi i18n orfane. Piano di lavoro in
    `NOTE-CLAUDE/REFACTORING-NUOVE-API/`.

2026-07-01  Alfonso Tatarelli <alfonso.tatarelli@link.it>

  * [Frontend] feat: refactoring della sezione Pendenze sulla nuova
    GovPay Console API V2. Nuovo client `ConsoleApiService`
    (`@core/services`) su base `GOVAPI.CONSOLE` (`/govpay-console-api`),
    con paginazione a slice (`Slice<T>` + `pagination.hasNextPage` +
    `nextCursor`) ed errori RFC 7807 (`problemDetail`). `ApiService`/V1
    resta per le altre feature (migrazione feature-by-feature).

  * [Frontend] feat(pendenze): lista e dettaglio migrati alla V2.
    Lista con i 4 filtri supportati (`idPendenza`, `numeroAvviso`,
    `idDominio` come select dai domini del profilo, `identificativoDebitore`
    con avviso audit GDPR), ordinamento `-dataUltimoAggiornamento`,
    totale via `total=true` sulla prima pagina. Dettaglio con voci inline,
    soggetto pagatore on-demand con consenso GDPR (`/informazioniDebitore`),
    stampa avviso PDF e ricevute guidati dagli HAL `_links`.

  * [Frontend] feat(shared-ui): nuovo `ConfirmDialogComponent`
    (`lnk-confirm-dialog`) e util `downloadBlob` nella libreria
    `@linkit/shared-ui`. NB: modifiche da replicare upstream
    (`[GOVPAY]/govpay-console`) prima del prossimo `sync:lib`.

  * [Frontend] chore: preset proxy `demo-v2` (`https://lab.link.it`) e
    script `npm run start:demo-v2`; chiave config `GOVAPI.CONSOLE` (dev+prod).
    Tipi V1 condivisi `DominioSummary`/`SoggettoPagatore` spostati in
    `@core/models`. Documentazione in
    `NOTE-CLAUDE/REFACTORING-NUOVE-API-PENDENZE/`.

2026-06-29  Alfonso Tatarelli <alfonso.tatarelli@link.it>

  * [Frontend] feat: adozione della libreria condivisa
    `@linkit/shared-ui` (v0.9.4) come copia mirror in
    `projects/lnk-shared-ui`. Il workspace diventa multi-progetto
    (app + libreria, build via ng-packagr). I componenti, le
    direttive, le utility (ex `@shared`) e i servizi `ConfigService`,
    `ThemeLoaderService`, `SystemFacade`, `LanguageService`
    (ex `@core/{config,system,i18n}`), più snackbar/spinner/tweaks/
    color-scheme-toggle/language-menu/goto-top/placeholder/
    maintenance/help-sidebar ora provengono dalla libreria. Rimossi
    i duplicati locali; in `@core/ui` resta solo `profile-menu`.

  * [Frontend] feat: i18n splittato in `src/i18n-domain/{it,en}.json`
    (chiavi di dominio) + i18n della libreria, mergiati al boot da
    `scripts/merge-i18n.js`. `assets/i18n/*.json` ora è generato
    (gitignored).

  * [Frontend] chore: script `scripts/sync-shared-ui.mjs` per
    sincronizzare la copia mirror dall'upstream
    `[GOVPAY]/govpay-console` e `scripts/resolve-i18n-conflict.mjs`
    per i conflitti i18n post-merge.

2026-05-01  Alfonso Tatarelli <alfonso.tatarelli@link.it>

  * [Frontend] Prima implementazione
